# 仓管系统网络安全审计报告

审计日期：2026-05-18  
审计方式：本地代码静态审计、配置审计、部署文档审计  
审计范围：`app.py`、`static/`、Docker/Windows 启动脚本、部署与数据安全文档  
未执行内容：未对正式服务器做攻击流量、爆破、爬取、破坏性压测；未读取或记录 `data/` 内真实业务数据、真实密码、真实 API Key。

## 2026-05-18 修复进展

本轮已完成第一批高优先级加固：

1. 新增 CSRF 双重提交令牌，HTML 页面自动注入 `csrf_token`，前端 `fetch(POST)` 自动附带 `X-CSRF-Token`。
2. 新增 Host Header 白名单校验，默认允许 localhost、绑定主机、服务器局域网 IP 和配置的公网域名。
3. 新增外部 API 请求安全校验，阻止回环、内网、链路本地、保留地址，并支持 `WAREHOUSE_ALLOWED_API_HOSTS` 白名单。
4. 管理员保存 AI/图片识别 API 配置前会先做 URL 安全校验。
5. 新增普通用户和管理员账号密码策略，普通用户至少 10 位，管理员至少 14 位，并拦截常见弱口令片段。
6. 清理库存图片识别结果和 LCSC 预览中的高风险 `innerHTML` 拼接点。

仍建议继续做第二批加固：全面清理前端剩余 `innerHTML`、把 CSP 改为 nonce/hash、反向代理强制 HTTPS、上传流式解析和全站错误回显脱敏。

## 总体结论

当前项目已经具备一批基础防护：登录鉴权、管理员下载限制、路径穿越检查、上传大小限制、密码 PBKDF2-SHA256 哈希、生产环境默认管理员密码拦截、`/data` AES-GCM 静态加密、`HttpOnly`/`SameSite=Lax` Cookie、常见安全响应头、库存搜索关键词与结果条数限制。

但从网络安防视角看，仍存在若干上线后应优先修复的风险：CSRF 校验不完整、Host Header 被信任、HTTP 明文部署风险、管理员可配置外部 API 带来的 SSRF 内网探测风险、前端仍存在多处 `innerHTML` 风险点、异常信息回显较多、上传处理可能被大请求拖慢。下面按风险级别逐条列出。

## 风险清单

| 编号 | 级别 | 风险点 | 建议优先级 |
|---|---|---|---|
| 1 | 高 | CSRF 防护不完整，缺少 Token 且允许无 Origin/Referer 的 POST | 立即修复 |
| 2 | 高 | Host Header 被用于同源判断，存在 Host 欺骗/DNS Rebinding 风险 | 立即修复 |
| 3 | 高 | 直接 HTTP/LAN 暴露时账号与会话可能明文传输 | 立即修复部署 |
| 4 | 中高 | 管理员配置的 AI/API 地址可触发服务端请求，存在 SSRF/内网探测风险 | 尽快修复 |
| 5 | 中高 | CSP 允许 `unsafe-inline`，前端多处 `innerHTML` 形成 XSS 风险面 | 尽快修复 |
| 6 | 中 | 大文件 multipart 读取进入内存，存在并发资源消耗风险 | 尽快修复 |
| 7 | 中 | `.env` 保存运行密钥与管理员密码，主机备份泄露会削弱加密收益 | 尽快加固 |
| 8 | 中 | 多个接口直接回显 `str(exc)`，可能泄露内部路径、上游 API 细节 | 计划修复 |
| 9 | 中 | 普通账号密码策略偏弱，仅要求 6 位 | 计划修复 |
| 10 | 低中 | 管理员界面可修改 `data_dir`、`host`、`public_url`，管理员会话被盗后影响面大 | 计划加固 |
| 11 | 低中 | 库存洞察接口对登录用户开放全局聚合数据 | 按业务确认 |
| 12 | 低 | 静态加密保护磁盘，不保护运行时内存与主机 Root/管理员权限 | 运维层面加固 |

## 1. CSRF 防护不完整

级别：高  
证据：`app.py:13768-13796`、`app.py:14154-14278`

当前 `verify_same_origin_post()` 逻辑为：没有 `Origin`/`Referer` 时直接放行，有来源时才与允许来源比对。所有重要 POST 路由在 `do_POST()` 开头统一调用该函数，包括登录、注册、库存录入、BOM 上传、管理员配置、用户创建、AI 配置等。

风险说明：

- 只依赖 `SameSite=Lax` Cookie 和可选的 `Origin`/`Referer` 不够完整。
- 某些客户端、代理、旧浏览器、特殊表单场景可能不带 `Origin`/`Referer`。
- 一旦用户已登录，攻击者可诱导其浏览恶意页面并提交跨站表单，造成库存修改、账号创建、配置变更等操作。

修复建议：

1. 为所有状态变更请求增加服务端生成的 CSRF Token。
2. Token 应绑定会话，写入页面隐藏字段或响应 JSON，再由前端通过 Header 提交。
3. POST/PUT/PATCH/DELETE 必须校验 Token；缺失或错误直接拒绝。
4. `Origin`/`Referer` 继续保留为辅助防线，但不能替代 Token。
5. 对 `/login`、`/register` 也建议校验 Token，避免登录 CSRF 和注册滥用。

## 2. Host Header 信任导致同源校验可被绕弱

级别：高  
证据：`app.py:13777-13792`

`allowed_request_origins()` 会读取请求头中的 `Host`，并把 `http://{host}`、`https://{host}` 加入允许来源。

风险说明：

- 如果服务直接暴露在内网或公网，攻击者可能构造带恶意 `Host` 的请求。
- 在 DNS Rebinding 或反向代理配置不严的情况下，Host 可能被攻击者控制，进而削弱同源判断。
- 该问题会放大第 1 条 CSRF 风险。

修复建议：

1. 增加固定的 `ALLOWED_HOSTS` 或 `WAREHOUSE_PUBLIC_ORIGIN` 配置。
2. 不要把任意请求 `Host` 自动加入可信来源。
3. 反向代理层开启 Host 白名单，只允许正式域名或固定内网 IP。
4. 应用层收到非白名单 Host 时返回 400。
5. 只允许一个生产公网 Origin，例如 `https://warehouse.example.com`。

## 3. 明文 HTTP 部署会暴露账号与会话

级别：高  
证据：`app.py:13727-13737`、`app.py:13761-13762`、`docker-compose.yml:8-20`、`app.py:15718-15727`、`SERVER_DEPLOY.md:60-66`

Cookie 只有在 `X-Forwarded-Proto=https` 或 `public_url` 是 HTTPS 时才会添加 `Secure`。Docker 默认监听 `0.0.0.0:8088`，Windows/LAN 部署也常见直接访问 `http://服务器IP:端口`。

风险说明：

- 直接使用 HTTP 时，登录密码、会话 Cookie、上传内容均可被同网段嗅探。
- 内网环境不等于安全环境，比赛队内网络、校园网、共享 Wi-Fi 都可能存在旁路监听。
- 一旦会话 Cookie 被截获，攻击者可冒充用户。

修复建议：

1. 正式环境必须放在 Nginx/Caddy/IIS 反向代理后，并启用 HTTPS。
2. 应用只监听 `127.0.0.1` 或 Docker 内部网络，由反向代理对外提供 443。
3. 配置 `public_url=https://正式域名`，确保 Cookie 自动带 `Secure`。
4. 反向代理设置 `X-Forwarded-Proto: https`。
5. 对公网部署强制 HSTS，不允许 HTTP 登录。

## 4. 管理员配置的外部 API 存在 SSRF 风险

级别：中高  
证据：`app.py:13041-13067`、`app.py:13109-13142`、`app.py:13156-13160`、`app.py:13186-13224`、`app.py:15064-15081`、`app.py:15539-15555`、`app.py:15615-15649`

系统允许管理员配置库存助手、图片识别 API 地址，并由后端调用 `urllib.request.urlopen()`。当前只校验地址以 `http://` 或 `https://` 开头。

风险说明：

- 如果管理员账号被盗，攻击者可把 API 地址设置为内网地址，利用服务器向内网服务发请求。
- 可能探测内网端口、云主机元数据地址、Docker 内部服务、路由器管理页等。
- 异常信息会返回给前端，可能帮助攻击者判断内网服务状态。

修复建议：

1. 增加出站请求域名白名单，例如只允许 OpenAI、DeepSeek、OpenRouter 或自建可信网关。
2. 禁止请求私有网段、回环地址、链路本地地址、组播地址和云元数据地址。
3. 解析域名后校验最终 IP，重定向后也要重新校验。
4. 禁止非标准端口，或只允许 443。
5. 对管理员 API 测试接口加审计日志，记录操作者、目标域名、时间和结果摘要。

## 5. CSP 允许内联脚本，前端存在多处 `innerHTML` 风险面

级别：中高  
证据：`app.py:13747-13759`、`static/inventory.js:296-306`、`static/inventory.js:380-388`、`static/analytics3d.js:24-38`、`static/analytics3d.js:72-74`、`static/location_2d.js:96-104`、`static/product_pcb_explosion.js:750-762`

当前 CSP 包含 `script-src 'self' 'unsafe-inline'` 与 `style-src 'self' 'unsafe-inline'`。前端多处使用 `innerHTML` 拼接 DOM，其中部分内容来自库存、LCSC 返回、AI 图片识别结果或错误信息。

风险说明：

- 如果某个库存字段、外部 API 返回字段、错误信息中混入 HTML/脚本片段，使用 `innerHTML` 时可能形成 XSS。
- `unsafe-inline` 会降低 CSP 的兜底保护能力；一旦存在 HTML 注入，内联脚本更容易执行。
- 当前部分位置使用了 `textContent` 或 `escapeHtml`，但风险面仍偏大，后续功能迭代容易踩坑。

修复建议：

1. 前端统一改为 `textContent`、`createElement()`、`setAttribute()` 组装 DOM。
2. 必须使用 HTML 模板时，先做严格 HTML 转义或使用可信模板函数。
3. URL 写入 `href` 前校验协议，只允许 `https://`、必要时允许 `http://` 的可信域名。
4. CSP 改为 nonce/hash 模式，逐步移除 `unsafe-inline`。
5. 增加前端 XSS 单元测试或 Playwright 安全用例，覆盖库存名称、备注、LCSC 字段、AI 返回字段。

## 6. multipart 上传整体读入内存，存在资源消耗风险

级别：中  
证据：`app.py:180-183`、`app.py:13955-13999`、`app.py:15718`

`read_multipart()` 根据 `Content-Length` 一次性读取整个请求体到内存，multipart 总上限为 100MB，PCB 文件上限为 80MB。服务使用 `ThreadingHTTPServer`，没有全局并发限制。

风险说明：

- 多个并发大文件上传可能快速消耗内存。
- 慢速上传可能长期占用线程。
- 若部署在小内存云主机或比赛现场普通电脑上，容易造成服务卡死。

修复建议：

1. 在反向代理层设置 `client_max_body_size`、请求速率限制和连接超时。
2. 应用层改用流式 multipart 解析，不把完整 body 一次性读入内存。
3. 为大文件上传设置独立并发上限。
4. 对普通用户上传频率做限流。
5. 将 100MB 总上限按真实业务再下调，或仅对管理员开放大文件上传。

## 7. `.env` 中保存运行密钥与管理员密码

级别：中  
证据：`configure_env.ps1:137-184`、`.gitignore`

配置脚本会把 `WAREHOUSE_ADMIN_PASSWORD` 与 `WAREHOUSE_DATA_KEY` 写入本地 `.env`。`.env` 已被 `.gitignore` 排除，不会上传 Git，但仍是服务器磁盘上的明文配置文件。

风险说明：

- 如果服务器账号、备份包、远程桌面、共享目录泄露，`.env` 泄露后攻击者可获得数据解密密钥。
- 数据文件即使是 AES-GCM 密文，只要密钥一起泄露，加密保护就失效。
- 管理员密码存在 `.env` 中，也会增加主机侧泄露后的账号风险。

修复建议：

1. 生产环境对 `.env` 设置最小权限，仅运行服务账号可读。
2. 备份时把数据备份和密钥备份分开存放。
3. Docker 部署优先使用 Docker secrets、系统环境变量、云厂商 Secret Manager 或 Windows DPAPI。
4. 不要把 `.env` 放入压缩发布包。
5. 密钥泄露时执行密钥轮换：停机备份、用旧密钥解密、用新密钥重新加密、废弃旧密钥。

## 8. 异常信息直接回显给用户

级别：中  
证据：`app.py:14490-14493`、`app.py:15080-15081`、`app.py:15359-15364`、`app.py:15579-15582`、`app.py:15645-15649`

多个接口在异常时直接返回 `str(exc)`。部分异常来自本地文件、外部 HTTP 请求、JSON 解析、PDF/PCB 分析等。

风险说明：

- 可能泄露内部文件路径、上游 API 响应、网络地址、HTTP 状态、库报错细节。
- 对攻击者来说，这些信息有助于枚举内部结构和判断攻击是否成功。

修复建议：

1. 对外返回统一错误码和简短中文提示。
2. 详细异常写入服务器日志，仅管理员可查看摘要。
3. 外部 API 错误只展示 HTTP 状态和“配置或网络异常”，不要透传上游完整响应。
4. 文件解析异常不要返回绝对路径。

## 9. 普通用户密码策略偏弱

级别：中  
证据：`app.py:15133-15162`、`app.py:15510-15528`

注册和管理员创建用户时只要求密码长度不少于 6 位。生产环境对默认管理员密码有拦截，但普通账号和新增管理员账号缺少强度要求。

风险说明：

- 弱密码容易被猜测或撞库。
- 登录失败有速率限制，但不能替代强密码策略。
- 普通账号也可能拥有 BOM、知识库、库存操作数据；新增管理员账号风险更高。

修复建议：

1. 普通用户最低 10-12 位，管理员最低 14 位。
2. 拦截常见弱密码、用户名同密码、连续数字、键盘序列。
3. 注册/创建用户时显示强度提示。
4. 增加管理员重置密码流程，而不是长期保存初始弱密码。
5. 重要部署建议增加二次验证或至少管理员登录二次确认。

## 10. 管理员配置项影响面过大

级别：低中  
证据：`app.py:13606-13631`、`app.py:15530-15537`

后台可保存 `host`、`port`、`data_dir`、`public_url`、注册开关、隧道模式等运行配置。保存后部分配置重启生效。

风险说明：

- 管理员账号被盗后，攻击者可修改监听地址、公开 URL、数据目录或注册策略。
- `data_dir` 指向异常位置可能导致重启后读写到错误目录，造成数据不可见或混乱。
- `public_url` 被恶意修改后，可能影响 Cookie Secure 判断和来源判断。

修复建议：

1. 将服务器级配置从普通后台拆出，仅超级管理员或本机配置文件可改。
2. 对 `data_dir` 做白名单或固定为环境变量，不允许网页修改。
3. 修改 `host`、`public_url`、`data_dir` 时要求再次输入管理员密码。
4. 对配置变更写入不可编辑审计日志。

## 11. 库存聚合洞察对所有登录用户开放

级别：低中，取决于业务规则  
证据：`app.py:14032-14033`、`app.py:12707-12787`

`/api/inventory_insights` 对任意登录用户返回全局库存分类、缺口、低库存和统计摘要。库存搜索本身已经做了“必须有具体关键词”和结果条数限制，见 `app.py:10734-10823`、`app.py:10990-11013`、`app.py:8986-9009`。

风险说明：

- 如果“所有用户都可搜索库存”是业务要求，则该接口不一定违规。
- 但聚合数据仍可让普通用户看到全局缺料、热门采购、低库存趋势。
- 若未来库存包含敏感器件或项目机密，聚合数据也可能泄露方向。

修复建议：

1. 明确业务规则：普通用户是否允许查看全局统计。
2. 如只允许搜索具体物品，则把洞察接口改为管理员可见，或只返回概览数字。
3. 对普通用户隐藏缺口名称、低库存名称，仅展示“存在缺口/需联系管理员”。
4. 对接口增加缓存和限流，避免反复枚举统计变化。

## 12. 静态加密的边界需要运维配合

级别：低  
证据：`app.py:391-408`、`app.py:441-456`、`app.py:489-499`、`app.py:511-563`、`DATA_SECURITY.md`

系统使用 AES-GCM 对 `data/` 文件与数据库做静态加密。加密数据库运行时会解密到内存 SQLite，提交时再序列化加密写回。

风险说明：

- 该机制主要防止“磁盘文件或离线备份泄露”。
- 如果攻击者已获得服务器管理员权限、运行进程权限或 `.env` 密钥，加密无法保护运行时数据。
- 这是静态加密的正常边界，不是实现错误，但需要运维上理解。

修复建议：

1. 服务账号最小权限运行，不用管理员/root 直接跑应用。
2. 服务器启用系统补丁、防病毒/EDR、远程登录强密码和最小开放端口。
3. 数据备份与密钥备份分离。
4. 对 `.env`、`data/`、备份目录设置访问控制。

## 已确认的有效防护

1. 下载接口需要登录且管理员权限，且使用 `Path(...).name` 与目录白名单防路径穿越：`app.py:15662-15697`。
2. 静态文件与 PCB 文件读取做了 `path_is_relative_to()` 检查：`app.py:14280-14295`、`app.py:14381-14410`。
3. PCB HTML 上传以 `text/plain` 返回，避免直接执行上传的 HTML：`app.py:14405-14410`。
4. 密码使用 PBKDF2-SHA256，并支持迭代次数升级：`app.py:582-607`。
5. 生产环境默认管理员密码会被拦截：`app.py:375-385`、`app.py:15087-15092`。
6. 库存搜索要求具体关键词，并限制单仓库结果条数，降低网页爬取原始数据库风险：`app.py:10734-10823`、`app.py:10990-11013`。
7. `.gitignore` 已排除 `.env`、`data/`、`tmp/`、备份、日志和压缩包，降低开源误传敏感数据风险。

## 建议修复顺序

第一优先级：

1. 加 CSRF Token。
2. 固定 Host/Origin 白名单。
3. 正式部署强制 HTTPS，应用不要直接公网裸露。
4. SSRF 出站请求白名单和私网 IP 拦截。

第二优先级：

1. 清理前端 `innerHTML`，收紧 CSP。
2. 反向代理限流、上传大小和超时控制。
3. 异常回显脱敏。
4. 强化普通用户和新增管理员密码策略。

第三优先级：

1. 管理员配置拆分为超级管理员/本机配置。
2. 普通用户可见统计范围重新确认。
3. `.env` 改用更强的密钥托管方案。
4. 增加安全回归测试。

## 上线部署安全基线

1. 只开放 80/443，应用端口 8088 不直接暴露公网。
2. HTTP 自动跳转 HTTPS。
3. 反向代理设置 Host 白名单。
4. 反向代理转发 `X-Forwarded-Proto: https`。
5. Docker 挂载独立 `data/`，不要把 `.env` 打进镜像。
6. `.env` 文件权限只允许服务账号读取。
7. 定期离线备份 `data/` 和单独备份 `WAREHOUSE_DATA_KEY`。
8. 管理员账号使用强密码，并关闭自助注册。
9. 保留访问日志，定期检查异常 POST、上传、登录失败和 API 配置变更。

## 结论

本项目已经具备可上线的基础安全框架，但还没有达到“强对抗环境”标准。上线前最需要补的是 CSRF、Host 白名单、HTTPS 强制和 SSRF 防护；这些问题属于网络边界和会话安全核心问题，应优先于界面或功能优化处理。
