# 仓管系统用户使用手册

版本日期：2026-05-16

本文档面向仓库系统的日常使用人员、硬件组成员、比赛物资责任人和管理员，说明如何访问系统、录入库存、检索物料、上传 BOM、管理比赛备件、查看报表以及处理常见问题。

## 1. 系统用途

仓管系统用于统一管理队内物料信息，减少人工查表和口头确认。系统主要解决以下问题：

- 日常库存在哪里、还有多少。
- 某个型号、封装或 LCSC 编号的器件是否已经有库存。
- 上传 BOM 后哪些器件库存足够，哪些需要采购。
- 比赛物资、机器人备件、贵重器材由谁负责。
- 仓库每周盘点和报表如何生成。
- 管理员如何维护用户、后台配置和访问记录。

系统内目前有两个独立仓库概念：

| 仓库 | 用途 | 位置字段含义 |
| --- | --- | --- |
| 原仓库 | 日常库存、电子元器件、普通物料入库和 BOM 对照 | 实际仓位 |
| 比赛备件仓库 | 机器人必备模块、比赛备件、机械/电控/贵重物资 | 责任人 / 组别 |

注意：比赛备件仓库和原仓库互相隔离，比赛物资不会自动写入日常库存，也不会自动生成原 BOM 采购单。

## 2. 访问和登录

### 2.1 内网访问地址

在同一个局域网内，浏览器输入：

```text
http://服务器局域网IP:8088
```

示例：

```text
http://192.168.1.25:8088
```

不要在自己的电脑或手机上输入：

```text
http://127.0.0.1:8088
```

`127.0.0.1` 只代表当前设备自己，只有在服务器本机打开浏览器时才使用。

### 2.2 登录账号

登录页面需要输入：

- 用户名。
- 密码。

管理员默认用户名是：

```text
admin
```

密码为服务器启动时设置的管理员密码。如果你是普通用户，请使用管理员分配给你的账号。

### 2.3 登录后看不到某些入口

这是权限控制，不是页面故障。

普通用户通常看不到：

- 用户管理。
- 后台配置。
- 采购收货复核。
- 库存调整审计。
- 焊接后台管理页。

如果确实需要权限，请联系管理员创建对应角色账号。

## 3. 页面导航说明

登录后顶部导航通常包括：

| 页面 | 主要用途 |
| --- | --- |
| 概览 | 查看库存总量、仓位数量、BOM 用量和常用入口 |
| 仓库检索 | 搜索原仓库或比赛备件仓库 |
| 填写表单 | 录入原仓库库存 |
| BOM对照 | 上传 BOM，和原仓库库存对照 |
| 比赛备件 | 管理比赛物资和机器人备件 |
| 统计排行 | 查看高频搜索、高需求缺料、BOM 用量排行 |
| 智能助手 | 围绕库存、BOM、器件知识进行问答 |
| 仓检报表 | 生成和下载仓检报表 |
| 用户 | 管理员创建用户 |
| 后台配置 | 管理员配置系统参数和 API |

建议普通用户的日常使用顺序：

1. 需要找物料：进入“仓库检索”。
2. 需要录入新库存：进入“填写表单”。
3. 需要检查 BOM 缺料：进入“BOM对照”。
4. 需要登记比赛物资：进入“比赛备件”。
5. 需要导出盘点信息：进入“仓检报表”。

## 4. 概览页面

入口：

```text
概览
```

概览页用于快速了解当前系统状态。

### 4.1 可以看到什么

概览页通常展示：

- 当前库存总数量。
- 库存条目数量。
- 仓位数量。
- BOM 累计用量。
- 最近入库记录。
- 常用功能入口。
- 当前内网访问地址或公网地址状态。

### 4.2 什么时候使用

以下情况适合先看概览：

- 想确认系统是否正常运行。
- 想快速进入入库、检索、BOM、报表等功能。
- 管理员想看当前库存总体状态。

## 5. 填写表单：录入原仓库库存

入口：

```text
填写表单
```

此页面用于录入日常原仓库库存。比赛物资不要在这里录入，应进入“比赛备件”。

### 5.1 适用物料

适合录入：

- 电阻、电容、电感。
- 芯片、模块、接口、连接器。
- 线材、端子、开关。
- 传感器、开发板、常规硬件物料。
- 日常仓库内有明确仓位的物品。

不建议录入：

- 某台机器人专属比赛备件。
- 需要责任人专管的贵重比赛物资。
- 只用于赛场的临时物资。

这些应录入“比赛备件”。

### 5.2 字段填写说明

常见字段含义：

| 字段 | 怎么填 | 示例 |
| --- | --- | --- |
| 商品类别 | 物料所属类别 | 电阻、电容、模块、连接器 |
| 商品名称 | 物料名称或型号 | 100nF 电容、降压模块 |
| 数量 | 实际入库数量 | 50 |
| 位置 | 实际仓位 | A1-L1-01 |
| 规格 | 阻值、容值、型号、电压等 | 100nF 50V |
| 封装 | 电子器件封装 | 0603、SOT-23 |
| LCSC 编号 | 立创商城编号 | C25803 |
| 品牌 | 厂牌或供应商 | YAGEO、MPS |
| 备注 | 额外说明 | 已拆封、散料、待复核 |

### 5.3 推荐录入规范

为了后续搜索和 BOM 对照准确，建议：

- 电阻写清阻值和封装，例如 `10k 0603`。
- 电容写清容值、电压和封装，例如 `100nF 50V 0603`。
- 模块写清输入输出和电流，例如 `24V转5V 5A 降压模块`。
- 仓位尽量统一格式，例如 `A1-L1-01`。
- 有 LCSC 编号时必须填写，例如 `C25803`。
- 数量只填写实际可用数量，不要把已损坏、已借出物料算进去。

### 5.4 LCSC 信息补全

如果物料有 LCSC 编号，可以输入编号后点击获取信息。系统会尝试自动补全：

- 商品名称。
- 商品类别。
- 规格。
- 封装。
- 品牌。
- 商品链接。

示例：

```text
C25803
```

注意：

- 自动补全结果需要人工检查。
- 如果编号填错，请修改编号后重新获取。
- 如果网络不可用，补全可能失败，但仍可手动填写。

### 5.5 图片识别辅助录入

如果管理员已经配置图片识别 API，页面会提供图片识别能力。

使用步骤：

1. 拍摄物料标签、料盘、包装袋。
2. 上传图片。
3. 等待识别结果。
4. 检查系统预填内容。
5. 修正错误字段。
6. 确认保存。

注意：图片识别只是辅助，最终数据以人工确认后的表单为准。

## 6. 仓库检索

入口：

```text
仓库检索
```

仓库检索用于查找物料是否存在、数量多少、在哪里或由谁负责。

### 6.1 选择搜索仓库

搜索前先选择范围：

| 选项 | 含义 |
| --- | --- |
| 原仓库 | 搜索日常库存 |
| 比赛备件仓库 | 搜索比赛物资、机器人备件 |
| 两个仓库同时搜索 | 同时搜索两边 |

如果不确定物料属于哪个仓库，可以选择“两个仓库同时搜索”。

### 6.2 搜索关键词怎么写

可以输入：

- 名称：`电容`、`降压模块`、`碳板`。
- 规格：`100nF`、`24V转5V`。
- 封装：`0603`、`SOT-23`。
- 编号：`C25803`。
- 仓位：`A1-L1-01`。
- 责任人：`张三`。
- 组别：`机械组`、`电控组`。

推荐搜索示例：

```text
100nF 0603
C25803
A1-L1-01
24V转5V
碳板 机器人1
张三 电控组
```

### 6.3 为什么不能空搜索

系统对所有登录用户开放库存搜索，但不会允许空搜索、通配符搜索或过宽条件批量枚举数据库。

这样做是为了：

- 防止网页爬取原始数据库。
- 防止误导出大量敏感库存信息。
- 保持查询结果更精准。

如果提示搜索范围过宽，请加入更具体的名称、封装、规格、编号或仓位。

### 6.4 搜索结果怎么看

原仓库结果重点看：

- 数量是否足够。
- 仓位是否明确。
- 规格和封装是否匹配。
- LCSC 编号是否一致。

比赛备件结果重点看：

- 责任人是谁。
- 责任人组别是什么。
- 物资属于哪台机器人或用途。
- 备注中是否有保管要求。

## 7. BOM 对照

入口：

```text
BOM对照
```

BOM 对照用于把项目 BOM 与原仓库库存进行匹配，判断哪些物料已有库存、哪些需要采购。

### 7.1 什么时候使用 BOM 对照

适用场景：

- 新 PCB 打样前检查库存。
- 焊接前确认器件是否齐全。
- 项目采购前统计缺料。
- 对比多个 BOM 的器件用量。

### 7.2 支持文件格式

支持：

```text
.csv
.xlsx
.txt
```

单个文件最大 12MB。如果文件过大，请拆分后上传。

### 7.3 推荐 BOM 字段

系统会尽量识别常见字段。推荐 BOM 中包含：

| 字段 | 含义 |
| --- | --- |
| Quantity | 数量 |
| Comment | 器件值或名称 |
| Designator | 位号 |
| Footprint | 封装 |
| Manufacturer Part | 厂商型号 |
| Supplier Part | 供应商编号 |
| LCSC Part | 立创编号 |

没有完全相同字段名时，系统也会尝试按常见别名识别。

### 7.4 上传流程

1. 进入“BOM对照”。
2. 拖拽或选择 BOM 文件。
3. 点击上传。
4. 等待系统解析。
5. 查看库存对照结果。
6. 记录缺料或生成采购需求。

### 7.5 对照结果说明

结果中常见列：

| 列 | 含义 |
| --- | --- |
| 类别 | 系统识别出的器件类别 |
| 名称 | BOM 中的物料名称 |
| BOM需求 | BOM 需要数量 |
| 库存 | 当前原仓库库存数量 |
| 缺口数量 | 需要补充采购的数量 |
| 原因 | 为什么需要采购或匹配失败 |

### 7.6 BOM 和比赛物资 BOM 的区别

非常重要：

- “BOM对照”上传的是项目 BOM，会和原仓库库存对照。
- “比赛备件”上传的是机器人必备模块 BOM，会进入比赛备件仓库。

两者数据互相隔离，不要混用。

## 8. PCB、PDF 与焊接工作台

BOM 上传后，系统可以关联 PCB、Gerber、HTML、PDF 等辅助文件。

### 8.1 适用场景

- PCB 文件和 BOM 绑定。
- 查看 BOM 与 PCB 关联信息。
- 对 PDF 原理图提交分析任务。
- 记录焊接任务状态。
- 焊接完成后按板数消耗库存。

### 8.2 焊接任务流程

建议流程：

1. 上传 BOM。
2. 关联 PCB、Gerber 或 PDF。
3. 打开 BOM/PCB 联动工作台。
4. 点击开始焊接。
5. 填写本次制板数量。
6. 焊接中途可以关闭页面。
7. 再次登录后，未完成任务仍会显示。
8. 完成后点击结束焊接。

注意：只有点击结束焊接后，流程才算完成，库存消耗才会进入记录。

## 9. 比赛物资备件管理

入口：

```text
比赛备件
```

此页面用于管理比赛相关物资和机器人专属备件。

### 9.1 适合录入哪些东西

适合录入：

- 两台机器人或多台机器人必备模块。
- 铝管、碳板、加工件。
- 电控模块、降压模块、线材。
- 摄像头、云台、电机、重要传感器。
- 赛场通用备件。
- 贵重物品。
- 需要专人保管的物资。

### 9.2 上传机器人必备模块 BOM

页面左侧提供“上传机器人必备模块 BOM”。

需要填写：

- BOM 标题。
- 机器人名称。
- 赛季或场次。
- 责任人。
- 责任人组别。
- BOM 文件。

上传后，这些物资会进入比赛备件仓库，不进入原 BOM 对照。

### 9.3 手动填写比赛物资

页面右侧提供“填写比赛物资表单”。

需要填写：

| 字段 | 说明 |
| --- | --- |
| 机器人/用途 | 物资属于哪台机器人或什么用途 |
| 类别 | 机械、电控、耗材、模块、贵重物品等 |
| 物资名称 | 具体名称 |
| 规格型号 | 尺寸、型号、参数 |
| 数量 | 当前需要或持有数量 |
| 单位 | 个、根、片、套等 |
| 责任人 | 专管人员姓名 |
| 责任人组别 | 机械组、电控组、硬件组等 |
| 硬件器件对照原仓库 | 是否只读检查原仓库是否有类似物料 |
| 备注 | 采购来源、保管要求、贵重物品编号等 |

### 9.4 责任人规则

比赛物资必须填写责任人。原因：

- 比赛物资经常是专用物资。
- 贵重器材需要明确保管人。
- 赛前检查需要按责任人快速追踪。
- 比赛现场丢失或借用后能及时确认。

### 9.5 和原仓库的关系

比赛备件仓库可以对照原仓库，但只是只读参考。

它不会：

- 自动扣减原仓库库存。
- 自动增加原仓库库存。
- 自动生成原采购单。
- 覆盖原仓库仓位信息。

## 10. 统计排行

入口：

```text
统计排行
```

统计排行用于判断哪些物料最常被查、最常缺、最常用于 BOM。

### 10.1 常见模块

可能包括：

- 高频搜索器件。
- 高需求缺料器件。
- 用户 BOM 用量排行。
- 库存分类分析。
- 低库存提醒。

### 10.2 使用建议

管理员可根据统计结果：

- 优先补充高需求缺料。
- 整理高频搜索但命名混乱的物料。
- 优化仓位和分类。
- 发现长期缺料或重复采购问题。

## 11. 智能助手

入口：

```text
智能助手
```

智能助手用于围绕库存、BOM、器件知识进行问答。

### 11.1 可以问什么

示例：

```text
当前 100nF 0603 电容库存够不够？
最近 BOM 里缺口最多的器件是什么？
C25803 是什么器件？
帮我总结我最近上传 BOM 常用的物料。
降压模块库存里有哪些规格？
```

### 11.2 回答依据

助手会尽量基于：

- 当前库存摘要。
- BOM 缺口。
- LCSC 缓存。
- 你的历史问答。
- 你的知识网络。

如果库存中没有依据，助手应说明缺少依据。

### 11.3 隐私边界

普通用户的助手历史和知识网络是用户私有的，其他普通用户不能查看。

## 12. 仓检报表

入口：

```text
仓检报表
```

仓检报表用于盘点和导出库存相关文件。

### 12.1 可做什么

通常可以：

- 立即生成周检报表。
- 查看历史报表。
- 下载全量库存对照表。
- 下载 LCSC 商品缓存。
- 刷新 LCSC 缓存。

### 12.2 什么时候生成报表

建议：

- 每周固定盘点前生成。
- 大批量入库后生成。
- 比赛前备货检查时生成。
- 上线或数据清理后生成。

### 12.3 报表保存位置

报表保存在服务器数据目录中：

```text
data/reports/
```

正式环境如果指定了独立数据目录，则在对应目录下的 `reports/` 中。

## 13. 用户管理

入口：

```text
用户
```

仅管理员可见。

### 13.1 创建用户

需要填写：

- 用户名。
- 密码。
- 角色。
- 组别。

角色可选：

- 普通用户。
- 管理员。

### 13.2 账号使用建议

- 每个人使用自己的账号，不要共用账号。
- 管理员账号只给需要后台权限的人。
- 普通录入和检索使用普通用户即可。
- 密码不要写在公开文档或群公告中。

## 14. 后台配置

入口：

```text
后台配置
```

仅管理员可见。

### 14.1 基础配置

可配置：

- 站点名称。
- 监听主机。
- 网站端口。
- 数据目录。
- 固定公网地址。
- 内网穿透开关。
- 注册开关。
- 高频搜索阈值。
- 高需求采购阈值。
- 低库存阈值。
- 每周仓检日。

端口、监听主机、数据目录、穿透方式保存后，需要重启服务才会生效。

### 14.2 监听主机怎么理解

常见值：

```text
127.0.0.1
```

只允许服务器本机访问。

```text
0.0.0.0
```

允许局域网其他设备访问。

服务器上双击 `run_windows.bat` 时，默认使用：

```text
0.0.0.0
```

### 14.3 注册开关

如果允许注册，用户可自行注册普通账号。正式使用时建议关闭公开注册，由管理员统一创建账号。

### 14.4 智能助手 API

配置项通常包括：

- API 地址。
- 模型名。
- API Key。
- 系统提示词。
- 知识深度。

未配置时，智能助手可能无法正常回答。

### 14.5 图片识别 API

用于“填写表单”页面的图片识别。未配置时不影响手动入库。

### 14.6 访问日志

访问日志用于排查：

- 谁登录过。
- 谁访问过某个页面。
- 请求是否报错。
- 是否存在异常访问。

## 15. 管理员后台扩展页面

部分后台页面不一定在顶部导航直接显示，但管理员可以从相关页面进入。

### 15.1 采购收货复核

用于处理 BOM 对照产生的采购项到货确认。

建议流程：

1. BOM 对照发现缺料。
2. 产生采购需求。
3. 实物到货后管理员复核。
4. 确认收货。
5. 系统写入库存调整记录。

### 15.2 库存调整审计

用于查看：

- 普通入库。
- 采购入库。
- 采购反冲。
- 焊接消耗。
- 手动调整。

管理员可用它追踪库存变化来源。

### 15.3 焊接后台

用于查看：

- 正在进行的焊接任务。
- 已完成的焊接任务。
- BOM 缺口。
- 采购待收。
- 后台分析任务。

## 16. 数据保存和备份

### 16.1 默认数据目录

默认数据目录：

```text
data/
```

重要内容：

```text
data/inventory.db.enc
data/inventory.db（旧版或关闭加密时）
data/forms/
data/reports/
data/uploads/
data/purchase_orders/
data/lcsc/
data/competition_materials/
data/user_knowledge/
```

### 16.2 备份建议

管理员应定期备份整个数据目录。建议：

- 每周至少备份一次。
- 大批量导入或上线更新前先备份。
- 比赛前备份一次完整数据。
- 不要只备份程序文件，真正的业务数据在 `data/` 目录。
- 如果已经启用数据加密，必须同时备份 `WAREHOUSE_DATA_KEY`，否则新服务器无法解密数据。

## 17. 上线更新和热补丁

### 17.1 完整更新

开发电脑生成压缩包：

```powershell
python build_zip.py
```

生成：

```text
warehouse_inventory_app.zip
```

服务器更新时：

1. 停止旧服务。
2. 备份数据目录。
3. 解压新程序。
4. 保留正式数据目录。
5. 启动服务。
6. 登录检查主要页面。

### 17.2 热补丁

生成补丁：

```powershell
python make_patch.py --all --name update-name
```

服务器应用补丁：

```powershell
powershell -ExecutionPolicy Bypass -File .\apply_patch.ps1 -PatchZip .\warehouse_patch_YYYYMMDD_HHMMSS_update-name.zip -AdminPassword "当前管理员强密码"
```

补丁脚本会尝试：

- 停止旧服务。
- 备份将被覆盖的程序文件。
- 覆盖新文件。
- 检查 Python 语法。
- 重启服务。
- 失败时回滚。

## 18. 常见问题

### 18.1 其他设备打不开系统

检查：

- 服务器程序窗口是否还开着。
- 控制台是否显示 `Bind: 0.0.0.0:8088`。
- 访问的是否是 `LAN` 地址。
- 两台设备是否在同一局域网。
- Windows 防火墙是否放行 Python 或 8088 端口。

### 18.2 只能服务器本机打开，其他电脑打不开

常见原因：

- 监听主机仍是 `127.0.0.1`。
- 没有用 `run_windows.bat` 启动。
- 防火墙拦截端口。
- 访问地址写成了 `127.0.0.1`。

正确做法：

```text
http://服务器局域网IP:8088
```

### 18.3 忘记管理员密码

使用新版 `run_windows.bat` 启动时，输入新的管理员密码。系统会把已有 `admin` 账号密码同步更新为本次输入的密码。

### 18.4 搜索不到物料

检查：

- 搜索范围是否正确。
- 是否在原仓库和比赛备件仓库之间选错。
- 关键词是否太宽或太少。
- 物料录入时名称、规格、封装是否完整。
- 是否有 LCSC 编号但未填写。

### 18.5 BOM 上传后结果不准确

可能原因：

- BOM 表头不规范。
- 数量列为空或格式异常。
- 封装、型号、LCSC 编号缺失。
- 库存录入时名称不一致。
- 物料实际属于比赛备件仓库而非原仓库。

处理建议：

- 检查 BOM 文件字段。
- 尽量保留 LCSC 编号。
- 在原仓库补充规格和封装。
- 对关键器件手动复核。

### 18.6 比赛备件为什么没有进入原仓库

这是设计如此。比赛备件仓库用于责任人专管，不应和日常库存混写。

如果某个比赛物资也确实进入了日常仓库，应在“填写表单”中另行录入原仓库库存。

### 18.7 图片识别不可用

可能原因：

- 管理员没有配置图片识别 API。
- API 地址错误。
- API Key 错误。
- 模型不支持图片输入。
- 网络不可达。

不影响手动入库。

### 18.8 智能助手不可用

可能原因：

- 管理员未配置助手 API。
- API Key 错误。
- 模型服务不可用。
- 网络不可达。

可以先使用普通检索和报表功能。

## 19. 日常使用规范

### 19.1 入库规范

- 新物料必须及时录入。
- 数量按实际可用数量填写。
- 仓位必须具体。
- 规格、封装、LCSC 编号尽量完整。
- 不确定信息写入备注，不要随意猜测。

### 19.2 搜索规范

- 优先用具体关键词。
- 搜不到时换用 LCSC 编号、封装、仓位。
- 不要使用空搜索或无意义关键词。
- 比赛物资优先搜索比赛备件仓库。

### 19.3 BOM 使用规范

- 上传前确认 BOM 文件是最终版本。
- 上传后检查缺口数量是否合理。
- 对关键器件手动复核库存。
- 不要把比赛物资 BOM 上传到普通 BOM 对照中。

### 19.4 比赛备件规范

- 每条比赛物资必须有责任人。
- 贵重物品必须写明保管要求。
- 机器人专属物资写清机器人名称。
- 通用备件写清用途。
- 赛前按责任人检查缺失项。

## 20. 快速操作清单

### 20.1 我要查一个物料有没有

1. 进入“仓库检索”。
2. 选择“原仓库”或“两个仓库同时搜索”。
3. 输入名称、规格、封装或 LCSC 编号。
4. 查看数量和位置。

### 20.2 我要录入新库存

1. 进入“填写表单”。
2. 填写名称、数量、位置。
3. 补充规格、封装、LCSC 编号。
4. 提交保存。
5. 回到“仓库检索”确认能搜到。

### 20.3 我要检查 BOM 缺料

1. 进入“BOM对照”。
2. 上传 `.csv`、`.xlsx` 或 `.txt` 文件。
3. 等待解析。
4. 查看缺口数量和采购建议。
5. 对关键器件手动复核。

### 20.4 我要登记比赛物资

1. 进入“比赛备件”。
2. 上传机器人必备模块 BOM，或手动填写比赛物资。
3. 填写责任人和组别。
4. 保存后到“仓库检索”选择比赛备件仓库复查。

### 20.5 我要导出仓检信息

1. 进入“仓检报表”。
2. 点击生成或下载已有报表。
3. 保存报表文件。
4. 管理员同步备份数据目录。
