# 更新日志

## 2026-05-31 BOM 对照、入库定位与登录来源修复

- BOM 对照改为保留封装参与分组和库存匹配，避免同名同规格但不同封装的器件被合并或被已有库存误判为已匹配。
- 库存录入的 2D 器件盒选择扩展到 30 个器件盒，并同步支持 `H30-xx-xx` 位置编码。
- 修复本地登录来源校验会丢失端口的问题，`http://127.0.0.1:8088` 和 `http://localhost:8088` 提交登录不会再被误报为不可信来源。
- 更新日志公告改为登录后全站展示，所有功能页都能收到最新系统公告；关闭逻辑仍按当天或当前版本记忆。

## 2026-05-19 网络安全防护加固

- 新增全站 CSRF 防护：登录、退出、后台配置、库存写入等会修改数据的请求都需要携带校验令牌，降低跨站请求伪造风险。
- 新增 Host Header 白名单校验，默认允许本机、局域网私有地址和配置的访问域名，降低 Host Header 投毒和异常域名访问风险。
- 外部 API 调用增加 SSRF 防护：阻止回连本机、内网、链路本地和保留地址，并支持通过 `WAREHOUSE_ALLOWED_API_HOSTS` 显式放行可信 API 域名。
- 管理员保存助手接口、图片识别接口等外部 URL 配置时会先做安全校验，避免把危险地址写入配置。
- 新增新账号/管理员密码强度校验，限制过短、常见弱口令和简单重复口令；已有用户密码和真实业务数据不会被清空或重置。
- 优化库存检索和 3D 分析前端渲染，减少动态 HTML 拼接带来的 XSS 风险。
- 新增 `static/security.js`，统一为前端修改类请求附加安全请求头；同步更新 `.env.example` 的安全配置说明。

## 2026-05-16 数据加密与安全加固

- 新增 `/data` 静态加密保护：默认启用 AES-256-GCM，`inventory.db` 会迁移为 `inventory.db.enc`，运行时在内存中查询，落盘时保持密文。
- API Key、图片识别配置、BOM 归档、PCB 关键帧、LCSC 缓存、上传文件、采购单、报表和用户知识文档改为加密写入，旧明文文件会在新版本首次启动时自动迁移。
- 用户密码继续使用不可逆 PBKDF2 哈希保存，不改成可解密密文；新密码哈希迭代次数提高到 600000 次，旧账号登录成功后自动升级。
- Docker、Windows、Linux 启动脚本新增 `WAREHOUSE_DATA_KEY` 要求；迁移加密数据时必须保留同一个密钥。

## 2026-05-16 Docker 部署与数据迁移

- 新增 Docker 部署文件：`Dockerfile`、`docker-compose.yml`、`.dockerignore`、`.env.example`、`requirements.txt`。
- 新增一键启动脚本：`docker_run.sh` 和 `docker_run.ps1`。
- 新增 `DOCKER_DEPLOY.md`，说明如何把旧服务器真实 `data/` 数据备份、恢复到新服务器，并通过 Docker 挂载运行。
- 新增 `backup_for_docker.py`，可把程序文件和正在运行中的真实数据打成完整 Docker 迁移包，其中 `inventory.db` 使用 SQLite 在线备份生成一致快照。
- 新增 `backup_for_docker.bat`，方便 Windows 服务器一键生成 Docker 完整迁移备份包。
- 上线压缩包同步纳入 Docker 部署文件，但不会把真实 `data/` 数据打进镜像。

## 2026-05-16 详细使用说明

- 新增 `WAREHOUSE_USER_MANUAL.md`，覆盖系统访问、账号权限、库存录入、仓库检索、BOM 对照、比赛备件、报表、后台配置、热补丁和常见问题。
- 扩展为面向最终用户的详细手册，增加字段填写规范、典型操作清单、比赛备件责任人规则、BOM 使用注意事项和排障流程。
- 上线压缩包纳入详细使用说明，服务器解压后可直接查看。

## 2026-05-16 后台监听地址显示修复

- 后台配置页现在优先显示当前进程真实运行的监听主机、端口、数据目录和穿透模式，避免旧配置文件里的 `127.0.0.1` 造成误判。
- `run_windows.bat` 启动时会同步写入 `server_config.json`，让后台保存配置和当前 Windows 服务器启动方式保持一致。
- `start.bat` 和打包提示统一指向 `run_windows.bat`，避免误用只监听本机的生产脚本。

## 2026-05-16 管理员密码与监听提示修复

- Windows 启动脚本在未设置管理员密码时会现场提示输入，并把该密码用于 `admin` 账号。
- 如果数据库里已经存在 `admin` 用户，启动时提供的 `WAREHOUSE_ADMIN_PASSWORD` 会同步更新该账号密码，避免初始化后忘记密码无法进后台。
- 启动日志新增 `Bind: host:port`，明确区分真实监听地址和本机浏览器访问地址。

## 2026-05-16 服务器映射修复

- `run_windows.bat` 现在默认监听 `0.0.0.0`，并把穿透模式设为自动，更适合直接在服务器上双击启动。
- 自动穿透判断改为支持环境变量覆盖配置文件，避免 `server_config.json` 里残留的 `tunnel_mode=off` 阻止映射。
- 如果服务器没有安装 `cloudflared` 或 `ngrok`，脚本会提示只能走局域网访问；外部访问还需要额外部署穿透工具或做路由器端口转发。

## 2026-05-16 上线压缩包同步

- 重新同步上线压缩包规则，保留最新比赛物资备件管理、仓库检索和页面样式改动。
- 将安全后的 `start.bat` 纳入部署包，启动时统一走生产脚本并要求设置 `WAREHOUSE_ADMIN_PASSWORD`。
- 修正 `stop.bat`，停止脚本现在按端口关闭当前 `python app.py` 仓库服务，不再执行旧的 Node 停止逻辑。

## 2026-05-16 比赛物资备件管理重做

- 重做比赛物资备件管理的数据边界：机器人必备模块 BOM、机械/电控/贵重物资表单全部写入独立比赛物资仓库，不再写入原 BOM 对照、原库存表或原采购单。
- 比赛物资表单新增责任人和责任人组别字段；比赛备件仓库的“位置”语义替换为对应责任人/组别，实现专人专管。
- 仓库检索新增“原仓库 / 比赛备件仓库 / 两个仓库同时搜索”选项；所有登录用户可自由搜索两个仓库，比赛备件结果显示责任人位置，硬件器件仅只读对照原仓库库存。

## 2026-05-15 库存系统上线公告

- 库存检索入口新增更新公告，进入仓库检索页时居中展示最新版本说明。
- 新增完整更新日志页面，后续每次上线更新都记录在本文件中。
- 公告关闭后会记住当前日志版本；本文件内容变化后，用户再次进入库存页会看到新的公告。

## 2026-05-12 上线前清理

- 清理临时截图、调试日志和本地测试输出，避免上线提交非必要文件。
- 重置业务数据目录，仅保留空数据库结构，等待真实物料清单录入。
- 调整启动逻辑，避免空库启动时自动生成无业务意义的空 CSV 和 Excel 报表。
